nya::button $title $size="m" $theme="normal" $type="button" $class $disabled $noAPI
    &"nya-button.css"
    &"nya-button.js"
    button%nya-button{size=$size}{theme=$theme}[type=$type]:if($disabled, [disabled]) AS $node
        @class +$class
        span AS $titleNode
            $title
    EXPOSE ($noAPI ? undefined : new Nya.Button($node, $titleNode))
