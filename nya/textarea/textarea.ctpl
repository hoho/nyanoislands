nya::textarea $name $value $size $placeholder $id $reset $rows $class $disabled $noAPI
    nya::_input $name $value $size (undefined) $placeholder $id $reset ($rows || true) $class $disabled $noAPI AS $ret
    EXPOSE $ret
